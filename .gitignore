# Secrets / local env
.env
*.env
!.env.example

# Go build & test artifacts
/backend/server
/backend/cmd/server/server
*.test
*.out

# Local Claude settings (keep shared commands tracked)
.claude/settings.local.json

# Local Docker Compose overrides (may hold local secrets/port remaps)
docker-compose.override.yml

# Editor / OS
.DS_Store
